# GymCore AI Services — Python Dependencies
# Pin versions for reproducible Docker builds.
# Update with: pip install --upgrade <package> && pip freeze > requirements.txt

fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1

# Anthropic Claude API — workout plan generation and form correction
anthropic==0.28.0

# Async HTTP client (used internally by the anthropic SDK)
httpx==0.27.0

# Multipart form support — required for future video/image file uploads
python-multipart==0.0.9
