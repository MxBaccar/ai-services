[mypy]
python_version = 3.11
strict = True
ignore_missing_imports = True

# FastAPI and Pydantic route decorators originate from packages whose stubs
# do not fully satisfy --disallow-untyped-decorators. This flag is relaxed
# so we can keep strict everywhere else without fighting the framework.
disallow_untyped_decorators = False
